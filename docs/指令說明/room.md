# /room 建立臨時文字頻道

## 功能概述
/room 用於建立僅特定使用者可見的臨時文字頻道。可與動態語音頻道搭配使用。

## 使用資格
- 一般使用者即可使用。

## 功能說明
輸入 `/room @使用者A @使用者B 時間` 後，系統會於指定分類下建立臨時頻道。時間使用 DHM 格式，例如：

- `1H`：一小時
- `30M`：三十分鐘
- `1D2H30M`：一天兩小時三十分鐘

頻道到期後系統會自動刪除。

## 操作流程
1. 輸入 `/room` 並選擇使用者。
2. 填寫存在時間。
3. 系統建立專屬臨時頻道。
4. 到期自動刪除。
